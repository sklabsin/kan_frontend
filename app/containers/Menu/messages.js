/*
 * Menu Messages
 *
 * This contains all the text for the Menu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Menu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Menu container!',
  },
});
