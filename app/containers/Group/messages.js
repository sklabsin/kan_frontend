/*
 * Group Messages
 *
 * This contains all the text for the Group container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Group';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Group container!',
  },
});
