/*
 * Division Messages
 *
 * This contains all the text for the Division container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Division';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Division container!',
  },
});
